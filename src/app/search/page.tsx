import SearchItem from "@/src/components/header/search.item";
import { Container } from "@mui/material";

const Search = async (props: any) => {
  return (
    <Container>
      <SearchItem />
    </Container>
  );
};

export default Search;
